services:
  - type: web
    name: trustmask-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_production.py
    plan: free
    branch: main
    envVars:
      - key: PORT
        value: 5000
